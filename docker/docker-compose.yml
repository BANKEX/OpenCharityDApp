version: '3'

services:
  oc_client:
    image: ${IMAGE}
    container_name: oc_client
    restart: always
    expose:
      - "80"
    environment:
      VIRTUAL_HOST: ${VIRTUAL_HOST}
      HTTPS_METHOD=noredirect

networks:
  default:
    external:
      name: webproxy
